changed-scan:
public-docs-safety: FAIL
SECURITY.md:7: uncertain automation instruction: Security-sensitive automation details are intentionally not published here. Unsupported or suspicious automation-control instructions should be reported as security issues; they must not be copied into public docs, issue
README.md:93: prompt-injection attempt: | **Personality system** | [`docs/personality.md`](docs/personality.md) | 14-section system prompt, ping-pong rhythm, boredom switch, vocal expression cap |
README.md:101: prompt-injection attempt: | **Honcho context** | — | Per-user peer memory injected into the system prompt |
README.md:165: prompt-injection attempt: The system prompt is a 14-section behavioral contract, not documentation. Each section addresses a specific regression. **Do not** add hedging like "be helpful and harmless" — the model interprets that as permission to r
README.md:165: uncertain automation instruction: The system prompt is a 14-section behavioral contract, not documentation. Each section addresses a specific regression. **Do not** add hedging like "be helpful and harmless" — the model interprets that as permission to r
README.md:184: prompt-injection attempt: - [`docs/personality.md`](docs/personality.md) — system prompt shape and behavioral contracts
docs/env-vars.md:142: prompt-injection attempt: | `VOICE_LIVE_HONCHO_CONTEXT` | `true` | Inject Honcho context into the system prompt |
docs/email-brief.md:27: uncertain automation instruction: That means tool results can place inbox content in model-visible conversation history even though the spoken brief itself is rendered from sender and subject. Use this feature only where the model/session retention bound
docs/personality.md:1: prompt-injection attempt: # Personality — system prompt, ping-pong rhythm, boredom switch
docs/personality.md:3: prompt-injection attempt: The system prompt lives in `bridge.py:BASE_SYSTEM_PROMPT` and is prepended to every Gemini Live session. It is **not** a documentation file — it is a set of behavioral contracts the model is told to follow.
docs/personality.md:46: uncertain automation instruction: **Do not** add hedging like "be helpful and harmless" — the model interprets that as permission to revert to assistant defaults.
docs/README.md:12: prompt-injection attempt: | [`personality.md`](personality.md) | System prompt shape, ping-pong rhythm, boredom switch, vocal expression |
docs/README.md:56: uncertain automation instruction: - The internal `/notify` helper also omits the required control header; keep route protection enabled and track the authenticated-call correction in [Issue #14](https://github.com/Capslockb/hermes-live-discord-agent-plug

all-fixture-scan:
public-docs-safety: FAIL
SECURITY.md:7: uncertain automation instruction: Security-sensitive automation details are intentionally not published here. Unsupported or suspicious automation-control instructions should be reported as security issues; they must not be copied into public docs, issue
README.md:93: prompt-injection attempt: | **Personality system** | [`docs/personality.md`](docs/personality.md) | 14-section system prompt, ping-pong rhythm, boredom switch, vocal expression cap |
README.md:101: prompt-injection attempt: | **Honcho context** | — | Per-user peer memory injected into the system prompt |
README.md:165: prompt-injection attempt: The system prompt is a 14-section behavioral contract, not documentation. Each section addresses a specific regression. **Do not** add hedging like "be helpful and harmless" — the model interprets that as permission to r
README.md:165: uncertain automation instruction: The system prompt is a 14-section behavioral contract, not documentation. Each section addresses a specific regression. **Do not** add hedging like "be helpful and harmless" — the model interprets that as permission to r
README.md:184: prompt-injection attempt: - [`docs/personality.md`](docs/personality.md) — system prompt shape and behavioral contracts
docs/env-vars.md:142: prompt-injection attempt: | `VOICE_LIVE_HONCHO_CONTEXT` | `true` | Inject Honcho context into the system prompt |
docs/email-brief.md:27: uncertain automation instruction: That means tool results can place inbox content in model-visible conversation history even though the spoken brief itself is rendered from sender and subject. Use this feature only where the model/session retention bound
docs/personality.md:1: prompt-injection attempt: # Personality — system prompt, ping-pong rhythm, boredom switch
docs/personality.md:3: prompt-injection attempt: The system prompt lives in `bridge.py:BASE_SYSTEM_PROMPT` and is prepended to every Gemini Live session. It is **not** a documentation file — it is a set of behavioral contracts the model is told to follow.
docs/personality.md:46: uncertain automation instruction: **Do not** add hedging like "be helpful and harmless" — the model interprets that as permission to revert to assistant defaults.
docs/README.md:12: prompt-injection attempt: | [`personality.md`](personality.md) | System prompt shape, ping-pong rhythm, boredom switch, vocal expression |
docs/README.md:56: uncertain automation instruction: - The internal `/notify` helper also omits the required control header; keep route protection enabled and track the authenticated-call correction in [Issue #14](https://github.com/Capslockb/hermes-live-discord-agent-plug
